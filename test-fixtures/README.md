# test-fixtures

This folder contains key pairs used for running tests, specifically for keys hardcoded in the Sanctum smart contracts.

Build the Sanctum `S` repository and copy the `.so` binaries into this folder.
